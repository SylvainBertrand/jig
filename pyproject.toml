[project]
name = "jig"
version = "0.1.0"
description = "Next-gen desktop GUI for robotics engineers"
requires-python = ">=3.12, <3.14"
dependencies = [
    "PySide6",
    "pyqtgraph",
    "mujoco",
    "mcap",
    "numpy",
    "Pillow",
]

[project.scripts]
jig-spike = "jig_spike_qt.main:main"
